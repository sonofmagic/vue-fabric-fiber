---
"vue-fabric-fiber": minor
---

Add origin-aware percent offsets to v-model snapshots and simplify the PDF watermark demo to bind X/Y percent directly from emitted `leftPercent`/`topPercent` values.
